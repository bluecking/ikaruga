language: cpp

addons:
  apt:
    packages:
    - libsdl2-dev
    - libsdl-gfx1.2-dev
    - libsdl-image1.2-dev
    - libsdl-mixer1.2-dev
    - libsdl-ocaml-dev
    - libsdl-sge-dev
    - libsdl-ttf2.0-dev
    - libsmpeg-dev

install: make get-deps


script: mkdir build && cd build && cmake ..
